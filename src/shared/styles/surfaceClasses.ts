/** Shared panel/surface shells — cards, loading panels, empty states. */

/** Solid panel shell — matches AppCard border/radius without padding or shadow. */
export const panelShellClass = 'rounded-xl border border-stone-200 bg-white'

/** Dashed empty-state shell — same border token as solid panels. */
export const dashedPanelShellClass = 'rounded-xl border border-dashed border-stone-200'

/** Tinted panel — filters and nested library controls. */
export const insetPanelShellClass = 'rounded-xl border border-stone-200 bg-stone-50/80'

/** Compact inset banner — filter summaries and inline notices. */
export const insetBannerClass = 'rounded-lg border border-stone-200 bg-stone-50'
