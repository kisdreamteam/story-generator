/** Shared dashboard page shells — width, padding, vertical rhythm. */

export const dashboardPageShellClass = 'mx-auto max-w-2xl px-1 sm:px-0'

/** Standard stacked dashboard content (library, detail, create preview). */
export const dashboardPageStackClass = `${dashboardPageShellClass} space-y-8`

/** Narrow empty-state panel on secondary routes (reader, export, roleplay). */
export const dashboardNarrowEmptyShellClass = `${dashboardPageShellClass} max-w-lg py-16`
